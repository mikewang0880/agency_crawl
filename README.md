Test Project for AgencyAnalytics
=======================

Requirements
============

* PHP >= 7
* Laravel >= 7

Installation
============

## Install

Run the docker composer


```
$ docker-compose up -d
```

The application can now be accessed at

Open browser `http://localhost:8000/`



